#include "main.h"

/**
 * _abs - check description
 * description: computes the absolute value of an integer
 * @r: integer input
 * Return: value of r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
