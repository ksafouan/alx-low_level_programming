#include "main.h"

/**
 * main - main function to get header ELF
 * @argc: count arguemnt
 * @argv: vector argument
 * Return: 1 in success, ERROR in failure
 */
int main(int argc, char *argv[])
{
	printf("argc:%d, argv:%p\n", argc, (void *)*argv);
	return (1);
}
